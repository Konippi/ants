import {FC, memo} from "react";

const Header: FC = memo(function Header() {
    
    return (
        <header style={{width:"100%", top:0, height:60, background:"red"}}>
            header
        </header>
    );
});

export default Header;
