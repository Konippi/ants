import React, { FC } from "react";

const Task: FC = () => {

    return (
        <>
            <div className="w-full h-screen flex justify-center items-center">
                Taskページ
            </div>
        </>
    );
};

export default Task;